{% extends "layout.html.twig" %}

{% block title %}Yhteystiedot{% endblock %}

{% block content %}
	{% include 'partials/navigation.html.twig' %}
	<main id="content">
		<div class="c-text-header v-bottom-space">
			<h1>Yhteystiedot</h1>
		</div>

		<div class="c-grid-container v-grid-2col-even">
			<div class="c-section v-full-content">
				{{ snippets.contactChairman|markdown }}
			</div>
			<div class="c-section v-full-content">
				{{ snippets.contactRegisterAdmin|markdown }}
			</div>
		</div>

		<div class="c-grid-container v-grid-2col-even v-bg-light">
			<div class="c-section v-full-content">
				{{ snippets.visitingAddress|markdown }}
			</div>
			<div class="c-section v-full-content">
				{{ snippets.mailingAddress|markdown }}
			</div>
		</div>

		<div class="c-grid-container v-grid-1col-narrow">
			<div class="c-section v-full-content">
				{{ snippets.contactTreasurer|markdown }}
			</div>
		</div>

	</main>
	{% include 'partials/footer.html.twig' with { variant: 'v-dark' } %}
{% endblock %}
